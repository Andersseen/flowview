{
  "@if": {
    "prefix": "@if",
    "body": ["@if (${1:condition}) {", "  $0", "}"],
    "description": "Flowmark if block"
  },
  "@for": {
    "prefix": "@for",
    "body": [
      "@for (${1:item} of ${2:ctx.items}; track ${1:item}.${3:id}) {",
      "  $0",
      "} @empty {",
      "  ",
      "}"
    ],
    "description": "Flowmark for block with empty branch"
  },
  "@switch": {
    "prefix": "@switch",
    "body": [
      "@switch (${1:expression}) {",
      "  @case (${2:value}) {",
      "    $0",
      "  }",
      "  @default {",
      "    ",
      "  }",
      "}"
    ],
    "description": "Flowmark switch block"
  },
  "interpolation": {
    "prefix": "{{",
    "body": ["{{ ${1:ctx.value} }}"],
    "description": "Flowmark escaped interpolation"
  }
}
