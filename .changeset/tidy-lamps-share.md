---
"@flowview/dom": minor
"@flowview/astro-events": patch
---

Add `@flowview/dom`, a tiny client-side DOM runtime that connects compiled Flowview render functions to native DOM targets. Also teach the Astro Events integration to install `.flow` event support so client-rendered Flowview DOM updates keep delegated handlers working.
